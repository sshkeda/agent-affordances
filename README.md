# agent-affordances

Generic capability/affordance definitions for agent tools, plus pure projections to MCP, Pi tool definitions, OpenAPI, and Markdown.

This package intentionally contains no product runtime logic. Product repos define their own tools/contracts on top of these primitives.
